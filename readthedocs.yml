version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "mambaforge-4.10"

python:
  # Install our python package before building the docs
  install:
    - method: setuptools
      path: .

conda:
    file: doc/environment.yml
